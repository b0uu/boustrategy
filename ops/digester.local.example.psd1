@{
    # Copy this file to digester.local.psd1 and replace the placeholder.
    # digester.local.psd1 is gitignored because the webhook URL is a secret.
    DiscordWebhookUrl = "https://discord.com/api/webhooks/REPLACE_ME"
}
